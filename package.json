{
  "name": "house-grocery-intelligence",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "node server.mjs",
    "dev:api": "node apps/api/src/server.js",
    "dev:app": "node server.mjs",
    "share:web": "npx.cmd localtunnel --port 3000",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "pdfkit": "^0.18.0"
  }
}
